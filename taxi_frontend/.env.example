# Frontend environment variables for RideConnect Taxi (Svelte + Vite)
# Base URL for REST API (defaults to http://localhost:3001)
VITE_API_BASE=http://localhost:3001

# Base URL for WebSocket server (defaults to ws://localhost:3001)
VITE_WS_BASE=ws://localhost:3001
